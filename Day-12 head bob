#include <iostream>
#include <string>

using namespace std;

int main() {
    int T;
    cin >> T; 
    cin.ignore(); 

    while (T--) {
        int N;
        cin >> N; 
        cin.ignore(); 

        string gestures;
        getline(cin, gestures); 

        if (gestures.find('I') != string::npos) {
            cout << "INDIAN" << endl;
        } else if (gestures.find('Y') != string::npos) {
            cout << "NOT INDIAN" << endl; 
        } else if (gestures.find('N') != string::npos) {
            cout << "NOT INDIAN" << endl; 
        } else {
            cout << "NOT SURE" << endl; 
        }
    }

    return 0;
}
