#include <iostream>
#include <vector>

using namespace std;

int main() {
    int T;
    cin >> T;
    
    while (T--) {
        int N, A;
        cin >> N >> A;
      
        int masks_required = min(A, N - A);
        
        cout << masks_required << endl;
    }
    
    return 0;
}
